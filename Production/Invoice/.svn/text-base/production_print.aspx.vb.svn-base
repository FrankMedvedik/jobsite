Imports CrystalDecisions.CrystalReports.Engine
Imports CrystalDecisions.Shared
Imports Microsoft.Data.SqlXml
Imports System.Data
Imports System.Data.SqlClient
Imports System.Configuration

Imports Microsoft.Reporting.WebForms
Imports System.Security.Principal
Imports System.Net
Imports Microsoft.ReportingServices


Public Class production_print
    Inherits System.Web.UI.Page

    'Protected WithEvents crReport As New ReportDocument()
    'Protected crExportOptions As ExportOptions
    'Protected crDiskFileOptions As DiskFileDestinationOptions

    'Dim fName As String

    Protected Sub Page_Init(ByVal o As Object, ByVal e As EventArgs) Handles MyBase.Init
        'InitializeComponent()
        'Dim xString As String = String.Empty
        'Dim sID() As String
        'Dim i As Integer
        'sID = Split(Request.Params("id"), ",")
        'For i = 0 To sID.GetUpperBound(0)
        '    If i = 0 Then
        '        xString = "id = '" + sID(i).ToString + "'"
        '    Else
        '        xString = xString + " or id = '" + sID(i).ToString + "'"
        '    End If
        'Next
        'xString = "invoice[" + xString + "]"

        'Dim oSQLXML As New SqlXmlCommand(WebConfigurationManager.AppSettings("xmlProback"))
        'oSQLXML.RootTag = "invoices"
        'oSQLXML.CommandText = xString
        'oSQLXML.CommandType = SqlXmlCommandType.XPath
        'oSQLXML.SchemaPath = Server.MapPath("~/Invoice/xml/invoice2.xsd")


        'Dim rdr As System.Xml.XmlReader = oSQLXML.ExecuteXmlReader
        'rdr.MoveToContent()
        'Dim ds As New DataSet()
        'Dim doc As New System.Xml.XmlDataDocument(ds)
        'doc.Load(rdr)

        'For cnt As Integer = 0 To ds.Tables.Count - 1
        '    Response.Write(ds.Tables(cnt).TableName)
        'Next
        
    End Sub


    Protected Sub Page_Load(ByVal sender As Object, ByVal e As System.EventArgs) Handles Me.Load


    End Sub

End Class


